namespace Constants {
    // svg constants

    export var SVG_NAMESPACE = "http://www.w3.org/2000/svg";
    export var SVG_INDENT = "  ";

    // canvas default values
    export var DEFAULT_CANVAS_WIDTH = 1000;
    export var DEFAULT_CANVAS_HEIGHT = 1000;
    export var DEFAULT_CANVAS_COLOUR = "white";

    // primitives
    export var DEFAULT_COLOUR = "green";
    export var DEFAULT_X = 0;
    export var DEFAULT_Y = 0;
    export var DEFAULT_HEIGHT = 100;
    export var DEFAULT_WIDTH = 100;

    export var DEFAULT_CIRCLE_SIDES = 100;

    // primitives - points
    export var PRIMITIVES_POINTS_CIRCLE_SIDES = 100;
    export enum PointsType {
        HollowCircle = 1,
        HollowDiamond = 2
    }

    // primitives - line
    export enum LineType {
        Default = 1
    }

    export var PolyLineTyp = {Default: "Default"};

    //
    export var POINTS_DEFAULT_TYPE_ID = 1;
    export var POINTS_DEFAULT_SIZE = 1;

}
